//undefined - a variable exist but at the moment it has no value
//undeclared - never formally declared in any scope we have access to, so it gives a reference error

let name;

console.log(name); // undefined

console.log(position); //ReferenceError: position is not defined
